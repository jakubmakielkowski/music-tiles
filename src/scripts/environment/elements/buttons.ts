import Button from "./classes/Button";
import StopButton from "./ui/controls/stopButton";
import PlayButton from "./ui/controls/playButton";

const buttons: Array<Button> = [
    StopButton,
    PlayButton,
];

export default buttons;